#include <iostream>
#include <fstream>
#include "Post.h"
using namespace std;


//THESE FUNCTIONS REMAIN TO BE CHECKED




/* Post::posting(){
	ofstream myfile("thepost.txt");
	if (myfile.is_open()){//If the file opens
		string buffer;
		cout << "Insert your post(note:ONE line message!)." << endl;
		cin >> buffer;//The user can post A SINGLE text line
		myfile << buffer;
		myfile.close();
	}
	else{
		cout << "Cannot open the file specified." << endl;
	}
}

*/